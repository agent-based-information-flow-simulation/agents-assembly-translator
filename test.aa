agent test
    prm yes, float, init, 5
    prm no, list, conn_list
    prm enum1, enum, yes, 100#, no 50
    prm enum2, enum, yes, 30, maybe, 70

    setupbehav setup_test
        action test
            decl tmp, 20
            ie yes, tmp
            eblock
        eaction
    ebehav
eagent

agent aa
eagent